export type Review = {
    id: number
    actor: string
    rating: number
    comment: string
}

export interface ReviewData {
    id: number
    actor: string
    rating: number
    comment: string
}