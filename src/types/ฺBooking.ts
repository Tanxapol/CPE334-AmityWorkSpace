export type BookingForm = {
    user: string[];
    Date: string;
    date: string;
    timestart: string;
    timeend: string;
}

export interface BookingFormData {
    user: string[];
    Date: string;
    date: string;
    timestart: string;
    timeend: string;
}